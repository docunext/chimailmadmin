.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", domains_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_domain_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Domain
    .inner
      = form_for :domain, :url => domains_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
